# Proxy compatibility

## 中文

VPS Scope 会把“识别到某个产品”和“能够可靠判断风险”分开。表格里的“已识别”不代表已经理解该产品的所有部署方式或数据库版本。

| 工作负载 | 当前覆盖 | 验证方式 |
|---|---|---|
| S-UI | 原生安装、面板/订阅/代理入口角色、数据库入口元数据、TCP/UDP 监听与 UFW 关系、数据库权限、内嵌 TLS 可见性、systemd 上下文 | Debian 13 真实 VPS；VLESS Reality、Hysteria2 和 Shadowsocks 已验证 |
| 3x-ui / x-ui | 原生安装、面板端口、动态数据库入口、监听与 UFW 暴露、生成的 Xray 配置、数据库权限、面板运行态一致性 | Ubuntu 22.04 上的 3x-ui v3.4.2；VLESS Reality、Trojan TLS、Shadowsocks TCP/UDP、VMess WS 已验证；x-ui 变体使用解析夹具 |
| sing-box | JSON 入口、Clash/V2Ray API、原生 `sing-box check`、敏感文件权限、systemd 上下文 | Debian 12 上的 sing-box 1.13.14 |
| Xray | JSON 入口、API inbound、原生 `xray run -test`、敏感文件权限 | 3x-ui v3.4.2 实际生成的 Xray 配置 |
| Hysteria2 | 原生 YAML 发现、sing-box Hysteria2 入口、UDP 运行上下文 | sing-box 1.13.14 真实入站与解析夹具 |
| TUIC | JSON 入口与 UDP 上下文 | 已有解析夹具，仍需真实主机验证 |
| Trojan / Shadowsocks | 原生 JSON 入口、TCP/UDP 语义、进程和常见配置发现 | 隐私夹具已覆盖；仍需真实主机验证 |
| Hiddify / Marzban / Outline | 进程、容器和发布端口证据 | 无法区分管理端点时保持 `UNKNOWN`，仍需更多真实面板布局 |
| WireGuard | 活动接口、UDP 监听、防火墙、peer 数量与近期握手数量、敏感文件线索 | 不导出 peer 公钥或 endpoint；仍需更多真实拓扑验证 |
| OpenVPN | 原生服务端配置中的监听地址、TCP/UDP 协议和端口关系 | 隐私夹具已覆盖；仍需真实主机验证 |
| Docker | 发布地址分类、privileged、host namespace、Docker socket 和宽泛挂载 | Ubuntu 26.04 真实 VPS 上的正常与故意风险容器 |

面板数据库由程序内置的只读 SQLite 读取器访问，不要求目标机安装 `sqlite3`。只执行固定的元数据查询，绝不导出 UUID、密码、私钥、Reality 参数或 TLS BLOB 内容。

测试场景包括：面板管理端口从受限到公网放行、代理 UDP 放行缺失、面板停止后的配置/监听不一致、Docker 回环发布、旧进程仍运行但配置已经损坏、短期 TLS 证书，以及必须确保不会进入报告的假密码和 Token。

提交兼容性问题时，请提供产品版本、安装方式、系统版本、相关服务或容器名称和脱敏报告。不要公开面板数据库或未经脱敏的配置。

## English

VPS Scope separates product detection from security conclusions. A row marked "detected" does not imply that every deployment layout or database version is understood.

| Workload | Current coverage | Validation |
|---|---|---|
| S-UI | Native installation, panel/subscription/proxy role mapping, database ingress metadata, TCP/UDP listener-to-UFW relations, database mode, embedded TLS visibility, systemd context | Real Debian 13 VPS; VLESS Reality, Hysteria2, and Shadowsocks validated |
| 3x-ui / x-ui | Native installation, panel port, dynamic database ingress, listener and UFW exposure, generated Xray config, database mode, panel runtime consistency | Real 3x-ui v3.4.2 on Ubuntu 22.04; VLESS Reality, Trojan TLS, Shadowsocks TCP/UDP, and VMess WS validated; parser fixtures for x-ui variants |
| sing-box | JSON ingress inventory, Clash/V2Ray API endpoints, native `sing-box check`, sensitive-file mode, systemd context | Real sing-box 1.13.14 on Debian 12 |
| Xray | JSON ingress and API-inbound inventory, native `xray run -test`, sensitive-file mode | Real Xray generated by 3x-ui v3.4.2 |
| Hysteria2 | Native YAML discovery and sing-box Hysteria2 ingress; UDP runtime context | Real Hysteria2 inbound on sing-box 1.13.14 plus parser fixtures |
| TUIC | JSON ingress and UDP context | Parser fixtures; real-host validation still needed |
| Trojan / Shadowsocks | Native JSON ingress, TCP/UDP semantics, process and common configuration discovery | Privacy fixtures covered; real-host validation still needed |
| Hiddify / Marzban / Outline | Process/container discovery and published-port evidence | Safe `UNKNOWN` when the management endpoint cannot be distinguished; real panel layouts still needed |
| WireGuard | Active interfaces, UDP listeners, firewall, peer and recent-handshake counts, secret-file signals | Peer keys and endpoints are never exported; more real-topology validation is needed |
| OpenVPN | Listener address, TCP/UDP protocol, and port relations from native server configuration | Privacy fixtures covered; real-host validation still needed |
| Docker | Published-address classification, privileged mode, host namespaces, Docker socket and broad binds | Real Ubuntu 26.04 VPS with safe and deliberately risky containers |

Panel databases are read through the program's built-in read-only SQLite reader; the target host does not need `sqlite3`. Only fixed metadata queries run, and UUIDs, passwords, private keys, Reality parameters, and TLS BLOB contents are never exported.

The test matrix deliberately includes panel management moving from restricted to public, missing UDP firewall coverage, a stopped panel with stale configuration, public wildcard listeners blocked by UFW, loopback Docker publication, broken configuration beside a still-running process, short-lived TLS certificates, and secret-bearing fixture values that must never appear in reports.

When reporting an unsupported layout, include the product version, installation method, OS version, relevant service/container names, and a redacted report. Never publish a panel database or unredacted configuration.
