# Proxy compatibility

## 中文

VPS Scope 会把“识别到某个产品”和“能够可靠判断风险”分开。表格里的“已识别”不代表已经理解该产品的所有部署方式或数据库版本。

| 工作负载 | 当前覆盖 | 验证方式 |
|---|---|---|
| S-UI | 版本化原生适配器、面板/订阅/代理入口角色、根/默认路径与 TLS 状态、数据库 schema 探测、TCP/UDP 监听与主机防火墙关系、角色冲突和残留监听、数据库权限、内嵌 TLS 可见性、systemd 上下文 | Debian 13 上的 S-UI 1.5.3 真实 VPS；VLESS Reality、Hysteria2 和 Shadowsocks 已验证；仓库保留无数据的 1.5.3 schema 夹具 |
| 3x-ui / x-ui | 版本化原生适配器、面板端口/路径/TLS/默认凭据状态、独立订阅端点与明文传输风险、数据库 schema 探测和动态入口、监听与主机防火墙暴露、生成的 Xray 配置、角色冲突和残留监听、数据库权限、面板运行态一致性 | Debian 12 与 Ubuntu 22.04 上的 3x-ui v3.4.2；VLESS Reality、Trojan TLS、Shadowsocks TCP/UDP、VMess WS 已验证；x-ui 变体使用匿名夹具 |
| sing-box | JSON 入口、Clash/V2Ray API、原生 `sing-box check`、敏感文件权限、systemd 上下文 | Debian 12 上的 sing-box 1.13.14 |
| Xray | JSON 入口、API inbound、原生 `xray run -test`、敏感文件权限 | 3x-ui v3.4.2 实际生成的 Xray 配置 |
| Hysteria2 | 原生 YAML 发现、sing-box Hysteria2 入口、UDP 运行上下文 | sing-box 1.13.14 真实入站与解析夹具 |
| TUIC | JSON 入口、UDP 监听、防火墙与运行上下文 | Debian 12 上的 sing-box 1.13.14 真实入站，并从另一台实验机验证 UDP 可达 |
| Trojan / Shadowsocks | 原生与 sing-box JSON 入口、TCP/UDP 语义、进程和常见配置发现 | Debian 12 上的 sing-box 1.13.14 真实入站；Trojan TCP 和 Shadowsocks TCP/UDP 已从其他实验机验证可达 |
| Hiddify | 版本化托管安装适配器、回环面板/控制接口、分片 sing-box/Xray 配置、Mieru TCP/UDP 端口绑定、监听和防火墙关系 | Ubuntu 22.04 上的 Hiddify Manager 12.3.3；已验证配置与实际 TCP/UDP 关系，且不导出用户或凭据 |
| Marzban | 版本化托管安装适配器、白名单 `.env` 语义、Uvicorn 管理端点、生成的 Xray 入口/控制接口、监听和防火墙关系 | Ubuntu 26.04 上的官方 Docker 部署；已验证管理面、Xray、TCP/UDP 和 host network 上下文 |
| Outline | 官方 Shadowbox 容器适配器、白名单环境语义、管理 API、TCP/UDP 密钥入口、监听与防火墙关系 | Ubuntu 26.04 官方 Docker 部署；公开管理 API 与正常代理入口已分别验证，API prefix 和密钥不进入报告 |
| WireGuard | 活动接口、UDP 监听、防火墙、peer 数量与近期握手数量、敏感文件线索 | Debian 12 与 Ubuntu 26.04 双机真实隧道；报告不导出 peer 公钥或 endpoint |
| OpenVPN | 原生服务端配置中的监听地址、TCP/UDP 协议和端口关系 | Debian 12 上的 OpenVPN 2.6 UDP 服务端与异机可达性验证 |
| Nginx / Caddy / HAProxy | 公网前端、后端监听、面板角色、路径路由管理面暴露与主机防火墙关系 | Marzban 的 Nginx、S-UI 的 Caddy、Hiddify 的 HAProxy 实机链路；外部伪装 upstream 不会按同端口误配到本机监听 |
| Docker | Compose 项目/服务关系、有效网络与挂载、发布地址分类、privileged、host namespace、Docker socket 和宽泛挂载 | Ubuntu 26.04 真实 VPS 上的正常与故意风险容器 |

面板数据库由程序内置的只读 SQLite 读取器访问，不要求目标机安装 `sqlite3`。只执行固定的元数据查询，绝不导出 UUID、密码、私钥、Reality 参数或 TLS BLOB 内容。

测试场景包括：面板管理端口从受限到公网放行、代理 UDP 放行缺失、面板停止后的配置/监听不一致、Docker 回环发布、旧进程仍运行但配置已经损坏、短期 TLS 证书，以及必须确保不会进入报告的假密码和 Token。

提交兼容性问题时，请提供产品版本、安装方式、系统版本、相关服务或容器名称和脱敏报告。不要公开面板数据库或未经脱敏的配置。

## English

VPS Scope separates product detection from security conclusions. A row marked "detected" does not imply that every deployment layout or database version is understood.

| Workload | Current coverage | Validation |
|---|---|---|
| S-UI | Versioned native adapter, panel/subscription/proxy role mapping, root/default-path and TLS posture, database schema probing, TCP/UDP listener-to-host-firewall relations, role collisions, stale listeners, database mode, embedded TLS visibility, systemd context | Real S-UI 1.5.3 on Debian 13; VLESS Reality, Hysteria2, and Shadowsocks validated; the repository keeps a data-free 1.5.3 schema fixture |
| 3x-ui / x-ui | Versioned native adapter, panel port/path/TLS/default-credential posture, separate subscription endpoint and plaintext-transport risk, database schema probing and dynamic ingress, listener and host-firewall exposure, generated Xray config, role collisions and stale listeners, database mode, panel runtime consistency | Real 3x-ui v3.4.2 on Debian 12 and Ubuntu 22.04; VLESS Reality, Trojan TLS, Shadowsocks TCP/UDP, and VMess WS validated; anonymous fixtures for x-ui variants |
| sing-box | JSON ingress inventory, Clash/V2Ray API endpoints, native `sing-box check`, sensitive-file mode, systemd context | Real sing-box 1.13.14 on Debian 12 |
| Xray | JSON ingress and API-inbound inventory, native `xray run -test`, sensitive-file mode | Real Xray generated by 3x-ui v3.4.2 |
| Hysteria2 | Native YAML discovery and sing-box Hysteria2 ingress; UDP runtime context | Real Hysteria2 inbound on sing-box 1.13.14 plus parser fixtures |
| TUIC | JSON ingress, UDP listener, firewall, and runtime context | Real sing-box 1.13.14 inbound on Debian 12 with UDP reachability observed from another lab host |
| Trojan / Shadowsocks | Native and sing-box JSON ingress, TCP/UDP semantics, process and common configuration discovery | Real sing-box 1.13.14 inbounds on Debian 12; Trojan TCP and Shadowsocks TCP/UDP observed from other lab hosts |
| Hiddify | Versioned managed-install adapter, loopback panel/control roles, fragmented sing-box/Xray configs, Mieru TCP/UDP port bindings, listener and firewall relations | Real Hiddify Manager 12.3.3 on Ubuntu 22.04; configured TCP/UDP relations validated without exporting users or credentials |
| Marzban | Versioned managed-install adapter, allowlisted `.env` semantics, Uvicorn management endpoint, generated Xray ingress/control parsing, listener and firewall relations | Real official Docker deployment on Ubuntu 26.04; management, Xray, TCP/UDP, and host-network context validated |
| Outline | Official Shadowbox container adapter, allowlisted environment semantics, management API, TCP/UDP key ingress, listener and firewall relations | Real official Docker deployment on Ubuntu 26.04; public management and expected proxy ingress validated separately without retaining API prefixes or keys |
| WireGuard | Active interfaces, UDP listeners, firewall, peer and recent-handshake counts, secret-file signals | Real two-host tunnel between Debian 12 and Ubuntu 26.04; peer keys and endpoints are never exported |
| OpenVPN | Listener address, TCP/UDP protocol, and port relations from native server configuration | Real OpenVPN 2.6 UDP server on Debian 12 with reachability observed from another lab host |
| Nginx / Caddy / HAProxy | Public frontend, backend listener, panel role, path-gated management exposure, and host-firewall relation | Real Nginx with Marzban, Caddy with S-UI, and HAProxy with Hiddify; external camouflage upstreams are not matched to local listeners by port alone |
| Docker | Compose project/service context, effective networks and mounts, published-address classification, privileged mode, host namespaces, Docker socket and broad binds | Real Ubuntu 26.04 VPS with safe and deliberately risky containers |

Panel databases are read through the program's built-in read-only SQLite reader; the target host does not need `sqlite3`. Only fixed metadata queries run, and UUIDs, passwords, private keys, Reality parameters, and TLS BLOB contents are never exported.

The test matrix deliberately includes panel management moving from restricted to public, missing UDP firewall coverage, a stopped panel with stale configuration, public wildcard listeners blocked by UFW, loopback Docker publication, broken configuration beside a still-running process, short-lived TLS certificates, and secret-bearing fixture values that must never appear in reports.

When reporting an unsupported layout, include the product version, installation method, OS version, relevant service/container names, and a redacted report. Never publish a panel database or unredacted configuration.
